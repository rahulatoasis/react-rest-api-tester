# react-rest-api-tester
REST API tester

Commands for creating react app
1. npm i -g create-react-app (install create-react-app globally if not installed already)
2. npx create-react-app my-react-app
